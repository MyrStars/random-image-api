# 随机图片API系统 - 开发文档

## 1. 项目概述

一个基于对象存储的随机图片API服务，支持多云存储后端（七牛云、阿里云OSS、腾讯云COS等），提供分类随机图片API和可视化管理后台。

### 核心功能

- **分类随机图片API**：`GET /api/{category}` 返回指定分类下的随机图片
- **多云存储适配**：统一抽象层，支持七牛云/阿里云OSS/腾讯云COS/Cloudflare R2/MinIO
- **管理后台**：分类管理、图片管理（上传/删除/预览）、存储源配置
- **图片预览**：管理后台内直接预览图片

---

## 2. 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 后端 | Node.js + Express | 与现有技术栈一致 |
| 数据库 | SQLite (sql.js) | 轻量级，无需额外部署 |
| 前端 | Vue 3 + Element Plus | 管理后台 |
| 存储SDK | 各云官方SDK | 通过适配器模式统一接口 |
| 部署 | Docker / 直接部署 | 灵活选择 |

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────┐
│                   客户端                          │
│  浏览器/APP/其他服务                               │
└──────────────┬──────────────────┬───────────────┘
               │                  │
       GET /api/{分类}        管理后台请求
               │                  │
               ▼                  ▼
┌──────────────────────────────────────────────────┐
│              Express Server (port 3100)           │
│                                                   │
│  ┌─────────────┐    ┌──────────────────────────┐ │
│  │  随机图片API  │    │     管理后台 API          │ │
│  │  /api/:cat   │    │  /admin/categories       │ │
│  │              │    │  /admin/images            │ │
│  │              │    │  /admin/storage           │ │
│  └──────┬───────┘    └────────────┬─────────────┘ │
│         │                         │               │
│         ▼                         ▼               │
│  ┌─────────────────────────────────────────────┐  │
│  │           Storage Adapter Layer              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐       │  │
│  │  │ 七牛云   │ │ 阿里OSS │ │ 腾讯COS │ ...   │  │
│  │  └─────────┘ └─────────┘ └─────────┘       │  │
│  └─────────────────────┬───────────────────────┘  │
│                        │                          │
│  ┌─────────────────────▼───────────────────────┐  │
│  │              SQLite Database                 │  │
│  │  categories / images / storage_configs       │  │
│  └─────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

---

## 4. 数据库设计

### 4.1 存储源配置表 `storage_configs`

```sql
CREATE TABLE storage_configs (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,              -- 存储源名称，如"七牛云主存储"
  type        TEXT NOT NULL,              -- 存储类型: qiniu / aliyun_oss / tencent_cos / cloudflare_r2 / minio
  config      TEXT NOT NULL,              -- JSON配置（bucket, region, accessKey等）
  endpoint    TEXT,                       -- 公开访问域名，如 https://cdn.example.com
  status      INTEGER DEFAULT 1,         -- 1=启用 0=禁用
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**各存储类型 config 字段结构：**

```json
// 七牛云
{
  "accessKey": "...",
  "secretKey": "...",
  "bucket": "my-images"
}

// 阿里云 OSS
{
  "accessKeyId": "...",
  "accessKeySecret": "...",
  "bucket": "my-images",
  "region": "oss-cn-hangzhou"
}

// 腾讯云 COS
{
  "secretId": "...",
  "secretKey": "...",
  "bucket": "my-images-1250000000",
  "region": "ap-guangzhou"
}

// Cloudflare R2
{
  "accountId": "...",
  "accessKeyId": "...",
  "secretAccessKey": "...",
  "bucket": "my-images"
}

// MinIO
{
  "endPoint": "minio.example.com",
  "port": 9000,
  "useSSL": false,
  "accessKey": "...",
  "secretKey": "...",
  "bucket": "my-images"
}
```

### 4.2 分类表 `categories`

```sql
CREATE TABLE categories (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  name            TEXT NOT NULL,           -- 分类名称
  slug            TEXT NOT NULL UNIQUE,    -- URL友好标识，如 "wallpaper", "avatar", 也可用数字 "1", "2"
  description     TEXT,                    -- 分类描述
  storage_id      INTEGER NOT NULL,        -- 关联的存储源ID
  storage_path    TEXT NOT NULL,           -- 存储桶内的目录前缀，如 "images/wallpaper/"
  status          INTEGER DEFAULT 1,       -- 1=启用 0=禁用
  cache_ttl       INTEGER DEFAULT 300,     -- 图片列表缓存时间（秒）
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (storage_id) REFERENCES storage_configs(id)
);
```

### 4.3 图片表 `images`

```sql
CREATE TABLE images (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  category_id     INTEGER NOT NULL,        -- 所属分类
  filename        TEXT NOT NULL,            -- 原始文件名
  storage_key     TEXT NOT NULL,            -- 存储桶中的完整路径/key
  url             TEXT NOT NULL,            -- 完整访问URL
  size            INTEGER,                 -- 文件大小(字节)
  width           INTEGER,                 -- 图片宽度
  height          INTEGER,                 -- 图片高度
  mime_type       TEXT,                    -- MIME类型
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES categories(id)
);

CREATE INDEX idx_images_category ON images(category_id);
```

---

## 5. API 设计

### 5.1 随机图片API（公开）

```
GET /api/{slug}
```

- **功能**：返回指定分类下的随机图片
- **响应**：302 重定向到图片URL（默认行为）
- **查询参数**：
  - `format=json` — 返回JSON而非重定向
  - `type=raw` — 返回原始图片（代理模式，图片从服务器中转）

**重定向模式（默认）：**
```http
GET /api/wallpaper
→ 302 Found
Location: https://cdn.example.com/images/wallpaper/abc.jpg
```

**JSON模式：**
```http
GET /api/wallpaper?format=json
→ 200 OK
{
  "code": 0,
  "data": {
    "url": "https://cdn.example.com/images/wallpaper/abc.jpg",
    "width": 1920,
    "height": 1080,
    "size": 245760
  }
}
```

**代理模式（图片中转，适用于防盗链场景）：**
```http
GET /api/wallpaper?type=raw
→ 200 OK
Content-Type: image/jpeg
[图片二进制数据]
```

### 5.2 管理后台API（需认证）

#### 存储源管理

```
GET    /admin/api/storages          -- 获取存储源列表
POST   /admin/api/storages          -- 添加存储源
PUT    /admin/api/storages/:id      -- 更新存储源
DELETE /admin/api/storages/:id      -- 删除存储源
POST   /admin/api/storages/:id/test -- 测试存储连接
```

#### 分类管理

```
GET    /admin/api/categories        -- 获取分类列表
POST   /admin/api/categories        -- 添加分类
PUT    /admin/api/categories/:id    -- 更新分类
DELETE /admin/api/categories/:id    -- 删除分类
```

#### 图片管理

```
GET    /admin/api/images?category_id=1&page=1&size=20  -- 获取图片列表（分页）
POST   /admin/api/images            -- 上传图片（支持批量）
DELETE /admin/api/images/:id        -- 删除图片（同时删除存储源文件）
POST   /admin/api/images/sync       -- 从存储源同步图片列表
```

---

## 6. 存储适配器设计

采用**适配器模式**，统一接口：

```javascript
// adapters/base.js
class StorageAdapter {
  constructor(config) { /* ... */ }

  // 上传文件
  async upload(key, buffer, mimeType) { throw new Error('Not implemented') }

  // 删除文件
  async delete(key) { throw new Error('Not implemented') }

  // 获取文件访问URL
  getUrl(key) { throw new Error('Not implemented') }

  // 列出目录下文件
  async list(prefix, marker, limit) { throw new Error('Not implemented') }

  // 测试连接
  async test() { throw new Error('Not implemented') }

  // 获取文件信息（可选）
  async stat(key) { throw new Error('Not implemented') }
}
```

**适配器注册表：**

```javascript
const adapters = {
  qiniu:        require('./adapters/qiniu'),
  aliyun_oss:   require('./adapters/aliyun_oss'),
  tencent_cos:  require('./adapters/tencent_cos'),
  cloudflare_r2:require('./adapters/cloudflare_r2'),
  minio:        require('./adapters/minio'),
};

function createAdapter(type, config) {
  const AdapterClass = adapters[type];
  if (!AdapterClass) throw new Error(`Unsupported storage type: ${type}`);
  return new AdapterClass(config);
}
```

### 各适配器依赖

| 存储类型 | NPM 包 |
|----------|---------|
| 七牛云 | `qiniu` (^7.x) |
| 阿里云OSS | `ali-oss` (^6.x) |
| 腾讯云COS | `cos-nodejs-sdk-v5` (^2.x) |
| Cloudflare R2 | `@aws-sdk/client-s3` (S3兼容) |
| MinIO | `minio` (^8.x) |

---

## 7. 随机图片选取策略

### 7.1 缓存 + 随机索引

```
请求到达 → 检查分类图片列表缓存
  ├─ 缓存命中 → 随机选取一张 → 重定向/返回
  └─ 缓存未命中 → 从数据库查询该分类所有图片URL → 写入缓存 → 随机选取
```

- 图片列表缓存时间由分类的 `cache_ttl` 控制（默认300秒）
- 新增/删除图片时主动失效缓存
- 使用内存Map缓存，key为分类slug

### 7.2 大量图片优化

当单个分类图片数量 > 10000 时：
- 数据库查询时使用 `ORDER BY RANDOM() LIMIT 1`（SQLite）
- 仅在缓存失效时重建索引

---

## 8. 前端管理后台

### 8.1 页面结构

```
/
├── /login                    -- 登录页
├── /dashboard                -- 仪表盘（图片总数、分类数、存储用量）
├── /storages                 -- 存储源管理
│   ├── 列表 + 状态指示
│   ├── 新增/编辑弹窗
│   └── 连接测试按钮
├── /categories               -- 分类管理
│   ├── 列表 + API地址复制
│   ├── 新增/编辑弹窗
│   └── 图片数量统计
└── /images                   -- 图片管理
    ├── 左侧分类树
    ├── 右侧瀑布流/网格预览
    ├── 拖拽上传 / 批量上传
    ├── 单图预览弹窗（放大、信息）
    └── 批量删除
```

### 8.2 图片预览功能

- **网格视图**：缩略图卡片，hover显示文件名和大小
- **预览弹窗**：点击放大查看原图，显示宽高/大小/URL
- **复制链接**：一键复制图片URL或Markdown格式
- **懒加载**：滚动加载，避免一次加载过多

---

## 9. 认证与安全

### 9.1 管理后台认证

- 基于用户名/密码的登录
- JWT Token 鉴权
- 环境变量配置管理员账号

### 9.2 API安全

- 随机图片API默认公开（无需认证）
- 可选：为每个分类设置独立的API Key
- 可选：Referer 白名单
- 可选：请求频率限制（rate limiting）

### 9.3 敏感信息

- 所有存储密钥加密后存入数据库
- 加密密钥通过环境变量 `ENCRYPT_KEY` 配置
- API响应中不暴露密钥信息

---

## 10. 项目结构

```
random-image-api/
├── server/
│   ├── index.js                  -- 入口
│   ├── config.js                 -- 配置加载
│   ├── database.js               -- SQLite初始化与表创建
│   ├── middleware/
│   │   ├── auth.js               -- JWT认证中间件
│   │   └── errorHandler.js       -- 统一错误处理
│   ├── routes/
│   │   ├── api.js                -- 公开随机图片API
│   │   ├── admin/
│   │   │   ├── storages.js       -- 存储源CRUD
│   │   │   ├── categories.js     -- 分类CRUD
│   │   │   ├── images.js         -- 图片管理
│   │   │   └── auth.js           -- 登录接口
│   │   └── index.js              -- 路由汇总
│   ├── adapters/
│   │   ├── base.js               -- 适配器基类
│   │   ├── qiniu.js              -- 七牛云
│   │   ├── aliyun_oss.js         -- 阿里云OSS
│   │   ├── tencent_cos.js        -- 腾讯云COS
│   │   ├── cloudflare_r2.js      -- Cloudflare R2
│   │   ├── minio.js              -- MinIO
│   │   └── index.js              -- 适配器注册表
│   ├── services/
│   │   ├── imageService.js       -- 图片业务逻辑
│   │   └── cacheService.js       -- 缓存管理
│   └── utils/
│       ├── crypto.js             -- 密钥加解密
│       └── imageInfo.js          -- 图片元数据解析
├── client/
│   ├── src/
│   │   ├── App.vue
│   │   ├── main.js
│   │   ├── router/index.js
│   │   ├── api/                  -- API请求封装
│   │   ├── views/
│   │   │   ├── Login.vue
│   │   │   ├── Dashboard.vue
│   │   │   ├── Storages.vue
│   │   │   ├── Categories.vue
│   │   │   └── Images.vue
│   │   └── components/
│   │       ├── ImageGrid.vue     -- 图片网格
│   │       ├── ImagePreview.vue  -- 图片预览弹窗
│   │       └── UploadDialog.vue  -- 上传组件
│   └── vite.config.js
├── .env.example                  -- 环境变量模板
├── package.json
└── README.md
```

---

## 11. 环境变量

```env
# 服务端口
PORT=3100

# 管理员账号
ADMIN_USER=admin
ADMIN_PASS=your_secure_password

# JWT密钥
JWT_SECRET=your_jwt_secret

# 存储密钥加密密钥（16/24/32字节）
ENCRYPT_KEY=your_16byte_key!!

# 数据库文件路径
DB_PATH=./data/images.db

# 公开访问地址（用于生成API地址）
PUBLIC_URL=https://abc.com
```

---

## 12. 部署方案

### 方案A：直接部署

```bash
npm install
npm run build:client    # 构建前端
npm start               # 启动服务
```

### 方案B：Docker

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY . .
RUN npm run build:client
EXPOSE 3100
CMD ["node", "server/index.js"]
```

---

## 13. 开发阶段划分

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| P0 | 数据库设计 + 基础框架 + SQLite初始化 | 高 |
| P1 | 存储适配器层（至少实现1个存储源） | 高 |
| P2 | 随机图片API（重定向 + JSON + 代理模式） | 高 |
| P3 | 管理后台API（存储源/分类/图片CRUD） | 高 |
| P4 | 前端管理后台（存储源/分类管理页面） | 高 |
| P5 | 图片上传 + 网格预览 + 预览弹窗 | 高 |
| P6 | 补充其他存储适配器 | 中 |
| P7 | 认证安全 + 频率限制 + Referer白名单 | 中 |
| P8 | 仪表盘统计 + 用量分析 | 低 |

---

## 14. 需求确认（已定）

| 项目 | 决定 |
|------|------|
| 存储源优先级 | 先实现七牛云 |
| 分类标识 | 支持自定义slug（如 `/wallpaper`, `/1`, `/abc`） |
| 返回模式 | 两种都支持，默认重定向，可选代理模式 |
| 前端技术栈 | Vue 3 + Element Plus |
| 部署方案 | Docker（方便部署到服务器） |
| 访问控制 | 支持API Key访问控制 |
| 图片元数据 | 自动解析图片宽高 |
| 批量导入 | 支持从存储源同步已有图片 + 本地上传 |
